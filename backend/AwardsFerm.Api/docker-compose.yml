services:
  api:
    build:
      context: ..
      dockerfile: backend/AwardsFerm.Api/Dockerfile
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      Worker__BaseUrl: http://host.docker.internal:8081
